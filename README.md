<div align="center">

# ⚔️ DisputesBot v0.11

**Telegram-бот для тренировки дебатов, логики и критического мышления**

![Python](https://img.shields.io/badge/Python-3.11%2B-blue)
![aiogram](https://img.shields.io/badge/aiogram-3.30.0-2CA5E0)
![PostgreSQL](https://img.shields.io/badge/PostgreSQL-Elo%20%26%20challenges-336791)
![Redis](https://img.shields.io/badge/Redis-live%20matches-red)
![Version](https://img.shields.io/badge/version-0.11.0-brightgreen)

</div>

## ✨ Новое в v0.11

- 🧭 рейтинговая калибровка по первым пяти завершённым матчам;
- 🏆 семь Elo-дивизионов: от Бронзы до Грандмастера;
- 📈 `/league` — дивизион, форма, Elo-дельта и прогресс повышения;
- 🥇 `/league_top` — таблица игроков с текущими дивизионами;
- 📊 `/league_distribution` — распределение игроков по лигам;
- ♻️ лиги вычисляются из существующих данных без новой миграции.

Все возможности v0.10 сохранены: долговечные персональные вызовы, автоматические
GitHub Releases, соперники, публичные профили с opt-in, косметика, daily-задания,
сезонный прогресс, Elo, модерация и одиночные споры.

## 🏆 Рейтинговые лиги

Первые пять завершённых PvP-матчей считаются калибровочными. Elo продолжает
изменяться обычным образом, но дивизион показывается только после завершения
калибровки.

| Дивизион | Минимальный Elo |
|---|---:|
| 🥉 Бронза | 0 |
| 🥈 Серебро | 900 |
| 🥇 Золото | 1000 |
| 💠 Платина | 1100 |
| 💎 Алмаз | 1200 |
| 🏅 Мастер | 1300 |
| 👑 Грандмастер | 1450 |

```text
/league               личный дивизион, место, форма и прогресс
/league_top           топ-10 текущего сезона с дивизионами
/league_distribution  количество и доля игроков в каждой лиге
```

`/league` показывает результаты последних пяти матчей в хронологическом порядке:
`В` — победа, `Н` — ничья, `П` — поражение. Elo-дельта учитывает только рейтинговые
матчи; нерейтинговые игры из anti-farming окна остаются в форме, но не меняют дельту.

Лига — это представление существующего сезонного Elo. Она не влияет на подбор,
судейство, daily-задания, награды, персональные вызовы или расчёт рейтинга.

## 🎯 Персональные PvP-вызовы

Вызовы сохраняются в PostgreSQL и не исчезают после перезапуска бота.

```text
/challenge тема                         reply на сообщение соперника
/challenge 123456789 тема               вызвать известного пользователя по ID
/challenges                             входящие и исходящие вызовы
/accept_challenge a1b2c3d4e5f6          принять вызов
/decline_challenge a1b2c3d4e5f6         отклонить входящий вызов
/cancel_challenge a1b2c3d4e5f6          отменить исходящий вызов
```

Пользователь, указанный по ID, должен уже иметь профиль в боте. При reply профиль
создаётся или обновляется из Telegram-данных сообщения.

### Жизненный цикл

1. Автор создаёт вызов с темой и получает короткий ID.
2. Соперник видит вызов в `/challenges` и получает личное уведомление, если бот может
   отправить ему сообщение.
3. При принятии вызов временно переходит в состояние `accepting`.
4. Существующий Redis-движок атомарно проверяет занятость игроков и создаёт матч.
5. После успешного старта вызов связывается с `match_id` и становится `accepted`.
6. При ошибке запуска резервирование снимается, и вызов снова можно принять.

Для одной пары и сезона может существовать только один активный вызов в любом
направлении. Зависшая резервация автоматически восстанавливается через пять минут.

## ⚔️ Соперники и личные встречи

```text
/rivals                     пять самых частых соперников
/head_to_head 123456789      личные встречи с игроком
/head_to_head                reply на сообщение игрока
```

`/head_to_head` показывает общий и рейтинговый счёт, Elo-дельту, текущую серию,
дату последней встречи и последние темы. Данные строятся только по завершённым матчам,
в которых участвовал сам запрашивающий пользователь.

## 🔐 Публичность PvP-профиля

Профиль приватный по умолчанию. Собственная карточка всегда доступна владельцу.

```text
/pvp_profile                         своя карточка
/pvp_profile 123456789               чужая публичная карточка
/pvp_profile                         reply на сообщение игрока
/profile_visibility                  текущая настройка
/profile_visibility public           разрешить просмотр
/profile_visibility private          скрыть профиль
```

Публичная карточка содержит Elo, место, сезонную статистику, очки и экипированную
косметику. Баланс токенов показывается только владельцу. Блокировка в любую сторону
запрещает просмотр профиля и персональные вызовы.

## 🛍 PvP-косметика

```text
/shop                 каталог, цены и требования
/buy sharp_mind       купить предмет по item ID
/inventory            показать купленные предметы
/equip sharp_mind     экипировать титул или значок
/unequip title        снять титул
/unequip badge        снять значок
```

Токены выдаются за daily-задания. Косметика не влияет на Elo, подбор соперника,
судейство или исход матча.

## 🎯 Ежедневные задания и сезон

```text
/daily        показать задания, прогресс и награды
/daily_claim  получить завершённые награды
/season       токены, очки, уровень и серии
/season_top   лидерборд сезонного прогресса
```

Набор заданий вычисляется по progression-дате и переживает перезапуск. Повторный claim
не начисляет награду второй раз.

## 📊 PvP-команды

| Команда | Назначение |
|---|---|
| `/duel [тема]` | Создать открытое приглашение |
| `/queue [тема]` | Встать в очередь подбора |
| `/leave_queue` | Выйти из очереди |
| `/rematch_duel [тема]` | Вызвать последнего соперника |
| `/challenge ...` | Создать персональный вызов |
| `/challenges` | Inbox и outbox вызовов |
| `/accept_challenge ID` | Принять вызов |
| `/decline_challenge ID` | Отклонить вызов |
| `/cancel_challenge ID` | Отменить вызов |
| `/duel_status` | Состояние активной дуэли |
| `/cancel_duel` | Отменить матч до первого хода |
| `/forfeit` | Сдаться |
| `/rating` | Личный Elo и место |
| `/league` | Дивизион, форма и прогресс повышения |
| `/league_top` | Таблица рейтинговых лиг |
| `/league_distribution` | Распределение игроков по лигам |
| `/pvp_leaderboard` | Топ-10 по Elo |
| `/duel_history` | История PvP-матчей |
| `/pvp_stats` | Расширенная аналитика |
| `/rivals` | Главные соперники |
| `/head_to_head user_id` | Личные встречи |
| `/pvp_profile [user_id]` | PvP-карточка |
| `/profile_visibility` | Публичность карточки |

## 🛡 Безопасность

| Команда | Назначение |
|---|---|
| `/block` | Заблокировать пользователя reply-сообщением |
| `/block user_id` | Заблокировать по Telegram ID |
| `/unblock user_id` | Удалить из blocklist |
| `/blocked` | Показать blocklist |
| `/report категория комментарий` | Пожаловаться на матч |
| `/my_reports` | Показать свои жалобы |

Blocklist проверяется при открытых приглашениях, очереди, рематчах, персональных
вызовах, принятии вызовов и просмотре чужого профиля.

## 🧠 Надёжность v0.11

- калибровка и дивизион вычисляются детерминированно из `rating` и `games`;
- рейтинг не дублируется в новой таблице и не может рассинхронизироваться;
- ранжирование использует Elo, число матчей, время обновления и user ID;
- форма читается только из неизменяемых завершённых матчей текущего сезона;
- Elo-дельта исключает нерейтинговые матчи;
- league-layer не читает стенограммы, аргументы или оценки судьи;
- вызовы сохраняются отдельно от краткоживущих Redis-приглашений;
- принятие вызова использует промежуточное состояние `accepting`;
- Redis user-locks исключают два одновременных матча одного пользователя;
- новых runtime-зависимостей и фоновых задач нет.

## 🗄️ Хранилища

### PostgreSQL

Профили, архивы, PvP Elo, завершённые матчи, blocklist, жалобы, progression wallets,
daily claims, косметика, loadout, публичность профиля и персональные вызовы.
Рейтинговые лиги отдельно не сохраняются.

### Redis

Активные споры и PvP-матчи, открытые быстрые приглашения, очередь, дедлайны,
request locks, rate limit и подтверждение удаления данных.

## ⚙️ Настройка вызовов

```env
PVP_CHALLENGE_TTL_HOURS=24
```

Допустимый диапазон: `1..168` часов.

## 🚀 Запуск

```bash
git clone https://github.com/Onmaynec/DisputesBot.git
cd DisputesBot
cp .env.example .env
```

Заполните `BOT_TOKEN`, `OPENAI_API_KEY` и при необходимости `MODERATOR_USER_IDS`.

```bash
docker compose up -d postgres redis
pip install -e ".[dev]"
alembic upgrade head
python -m bot.main
```

Полный запуск в Docker:

```bash
docker compose up --build -d
```

## ⬆️ Обновление с v0.10

```bash
git pull
pip install -e ".[dev]"
alembic upgrade head
```

v0.11 не добавляет таблиц и не изменяет схему. `alembic upgrade head` подтверждает,
что база находится на миграции `0007_challenges`. Существующие профили, вызовы,
матчи, Elo, токены, косметика, blocklist и social-настройки не изменяются.

## 📦 Релизы

Workflow `.github/workflows/release.yml` запускается после push в `main`:

1. читает версию из `pyproject.toml`;
2. проверяет наличие GitHub Release с соответствующим тегом;
3. собирает wheel и source distribution;
4. извлекает notes текущей версии из `CHANGELOG.md`;
5. создаёт тег `vX.Y.Z` и GitHub Release с артефактами.

Повторный push с той же версией безопасен: существующий Release не создаётся заново.

## 🧪 Проверки

```bash
ruff check .
python -m compileall -q bot tests migrations
alembic upgrade head
pytest -q
```

GitHub Actions выполняет проверки с PostgreSQL 17 на Python 3.11 и 3.13.

## 🔐 Приватность

`/delete_me` удаляет профиль, архивы, PvP-рейтинг, историю матчей, blocklist,
progression wallet, daily claims, косметику, публичность, входящие и исходящие вызовы,
настройки и активные Redis-сессии. Жалобы остаются обезличенными аудиторскими записями.

Вызов хранит только ID участников, сезон, тему, статус, срок действия и связанный
`match_id` после принятия. Лиги не хранят отдельные персональные данные: название
дивизиона, прогресс и распределение вычисляются при запросе из агрегатов Elo и матчей.

## 📄 Лицензия

MIT.
